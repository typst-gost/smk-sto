# smk-sto-004

Typst-шаблон для оформления отчётов о лабораторных работах в соответствии
со стандартом организации **СМК СТО 004–2020** «Правила оформления
отчётов о лабораторных работах. Общие требования к рефератам и
аннотациям» ФГБОУ ВО «МГУ им. Н.П. Огарёва».

Шаблон закрывает все требования стандарта по оформлению: поля страницы,
шрифт, межстрочный интервал, абзацный отступ, заголовки, перечисления,
формулы, рисунки, таблицы, приложения, а также автоматически собирает
титульный лист по форме из Приложения А.

## Быстрый старт

```typst
#import "@preview/smk-sto-004:0.1.0": *

#show: lab-report.with(
  institute: "Институт электроники и светотехники",
  department: "Кафедра метрологии, стандартизации и сертификации",
  work-number: 1,
  discipline: "Поверка средств измерений электрических величин",
  title: "Измерение в цепях постоянного тока",
  author: (
    name: "И.О. Фамилия",
    direction: "27.03.01 Стандартизация и метрология",
  ),
  supervisor: (
    name: "И.О. Фамилия",
    position: "канд. техн. наук, доц.",
  ),
  designation: (direction: "27.03.01", variant: "08"),
)

#outline()

= Цель работы
Изучить ...

= Программа и методика эксперимента
...

= Результаты измерений
#figure(
  table(
    columns: 3,
    [Опыт], [$U$, В], [$I$, А],
    [1], [5,0], [0,10],
  ),
  caption: [Результаты измерений],
) <tab:results>

= Обработка результатов
$ R = U / I $ <eq:ohm>

где $U$ — напряжение, В; \
    $I$ — сила тока, А.

В соответствии с формулой @eq:ohm ...

= Выводы
...

#bibliography("references.bib",
  style: "gost-r-705-2008-numeric",
  title: [Список использованных источников])

#show: appendix
= Дополнительные расчёты
...
```

## Блок-схемы и графы

Шаблон переэкспортирует `fletcher` — декларативные блок-схемы прямо в
Typst (узлы и стрелки описываются кодом, без drawio/Visio):

```typst
#figure(
  diagram(
    node((0, 0), [contur2 (app)], fill: rgb("#dae8fc")),
    node((2, 0), [contur2_demos],  fill: rgb("#d5e8d4")),
    node((1, 1), [contur2_lib],    fill: rgb("#ffe6cc")),
    node((3, 1), [tests (GTest)],  fill: rgb("#f8cecc")),
    edge((0, 0), (1, 1), "->", [link]),
    edge((2, 0), (1, 1), "->", [link]),
    edge((3, 1), (1, 1), "->", [test]),
  ),
  caption: [Граф зависимостей модулей],
) <fig:deps>
```

Узлы по умолчанию — скруглённые прямоугольники с тонкой чёрной обводкой
(совместимо с СТО 8.5). Полный API: см. документацию пакета
[fletcher](https://typst.app/universe/package/fletcher).

## Возможности

- Автоматическая сборка титульного листа по Приложению А.
- Поля 30/15/20/20 мм, Times New Roman 14 pt, межстрочный интервал 1,5.
- Нумерация разделов «1», «1.1», «1.1.1», абзацный отступ 1,25 см.
- Структурные заголовки (Содержание, Введение, Заключение, Список
  использованных источников и др.) автоматически центрируются и
  выводятся заглавными.
- Маркер списка «–», нумерация перечислений «1)», поддержка алфавитных
  пунктов «а)», «б)», … (`enum-letter`).
- Подпись таблицы с разрядкой букв «Т а б л и ц а» по СТО 8.6.5.
- Подпись рисунка «Рисунок N – ...» по СТО 8.5.
- Нумерация формул «(1)» справа, ссылки вида «(1)» по СТО 8.4.4.
- Приложения через `#show: appendix`: автоматическая буквенная
  нумерация (А, Б, В, …), отдельная перенумерация рисунков, таблиц и
  формул в каждом приложении.
- Автоматическое формирование обозначения ЛР: достаточно передать
  `(direction: "27.03.01", variant: "08")` и шаблон соберёт строку
  «ЛР–02069964–27.03.01–08–YY».

## Параметры `lab-report`

| Параметр        | Тип                | Назначение                                                                  |
|-----------------|--------------------|-----------------------------------------------------------------------------|
| `institute`     | строка             | Институт / факультет / филиал.                                              |
| `department`    | строка             | Кафедра.                                                                    |
| `work-number`   | число / строка     | Номер лабораторной работы.                                                  |
| `discipline`    | строка             | Дисциплина.                                                                 |
| `title`         | строка             | Название работы (печатается заглавными).                                    |
| `author`        | словарь            | `(name: "...", direction: "...")`.                                          |
| `supervisor`    | словарь            | `(name: "...", position: "...")`.                                           |
| `designation`   | строка / словарь   | Готовая строка «ЛР–...» либо `(direction: "...", variant: "...", year: ...)`.|
| `ministry`      | строка             | По умолчанию — Минобрнауки РФ.                                              |
| `organization`  | словарь            | По умолчанию — МГУ им. Н.П. Огарёва.                                        |
| `city`          | строка             | По умолчанию — «Саранск».                                                   |
| `year`          | число / `auto`     | По умолчанию — текущий год.                                                 |
| `hide-title`    | `bool`             | Скрыть титульный лист (например, для отладки).                              |
| `add-pagebreaks`| `bool`             | Авто-разрывы страниц перед разделами 1-го уровня.                           |
| `text-size`     | length             | По умолчанию 14 pt.                                                         |
| `indent`        | length             | Абзацный отступ, по умолчанию 1,25 см.                                      |
| `margin`        | dictionary         | Поля страницы, по умолчанию 30/15/20/20 мм.                                 |

## Структура отчёта по СТО

Согласно п. 7.1 СТО 004–2020 отчёт должен содержать:

1. Титульный лист (формируется автоматически).
2. Цель или задачи работы.
3. Программа и методика эксперимента.
4. Результаты измерений (наблюдений).
5. Обработка экспериментальных данных и оценка погрешностей.
6. Анализ результатов и выводы (заключение).

Опционально: список использованных источников и приложения.

## Локальная разработка

### Быстрая проверка изменений

```bash
typst compile --root . template/example.typ
```

Файл `template/example.typ` импортирует пакет из `src/lib.typ` напрямую
(минуя `@preview`) — это удобно при разработке: правка → пересборка
без копирования файлов.

### Установка в локальный кэш `@preview`

Чтобы протестировать пакет так, как его увидят пользователи после
публикации в [Typst Universe](https://typst.app/universe) — кладём его
в локальный кэш `@preview`. После этого `#import "@preview/smk-sto-004:0.1.0"`
работает из **любого** проекта на этой машине.

Путь к кэшу зависит от ОС:

| ОС       | Путь                                                              |
|----------|-------------------------------------------------------------------|
| Windows  | `%APPDATA%\typst\packages\preview\smk-sto-004\0.1.0\`             |
| Linux    | `~/.local/share/typst/packages/preview/smk-sto-004/0.1.0/`        |
| macOS    | `~/Library/Caches/typst/packages/preview/smk-sto-004/0.1.0/`      |

**Windows (PowerShell):**

```powershell
$dest = "$env:APPDATA\typst\packages\preview\smk-sto-004\0.1.0"
Remove-Item -Recurse -Force $dest -ErrorAction SilentlyContinue
New-Item -ItemType Directory -Force -Path $dest | Out-Null
robocopy . $dest /E `
    /XF "standard.pdf" "AGENTS.md" "example.typ" "example.pdf" ".gitignore" `
    /XD ".git" ".github" "temp-render" ".claude" reference | Out-Null
```

**Linux / macOS:**

```bash
DEST="${XDG_DATA_HOME:-$HOME/.local/share}/typst/packages/preview/smk-sto-004/0.1.0"
rm -rf "$DEST"
mkdir -p "$DEST"
rsync -a \
    --exclude='.git' --exclude='.github' --exclude='reference' \
    --exclude='standard.pdf' --exclude='standard.txt' \
    --exclude='AGENTS.md' --exclude='temp-render' \
    --exclude='template/example.typ' --exclude='template/example.pdf' \
    ./ "$DEST/"
```

Список исключений в обеих командах согласован с полем `exclude` в
[`typst.toml`](typst.toml).

### Проверка из стороннего проекта

После установки в кэш:

```bash
mkdir /tmp/check && cd /tmp/check
echo '#import "@preview/smk-sto-004:0.1.0": *
#show: lab-report.with(work-number: 1, title: "Проверка",
  designation: (direction: "27.03.01", variant: "01"))
= Раздел
Текст.' > report.typ
typst compile report.typ
```

Если PDF собрался — пакет корректно установлен и готов к использованию.
Тем же путём (`typst init @preview/smk-sto-004 my-report`) можно
создать новый проект из вшитого в пакет шаблона.

### Обновление пакета в кэше

После любых правок исходников просто повторите команду установки. Кэш
полностью заменяется — никакой ручной чистки не нужно.

## Лицензия

MIT — см. файл [LICENSE](LICENSE).

## Источник

[СМК СТО 004–2020](standard.pdf) — стандарт организации ФГБОУ ВО
«МГУ им. Н.П. Огарёва», утверждённый приказом № 514 от 15.09.2020 г.
